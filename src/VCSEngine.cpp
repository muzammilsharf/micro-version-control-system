/*Implement the VCSEngine class.
1. commit(repo, message): Deep copy the working_directory map into a new CommitNode, generate a unique hash, link parent_hash, append to the repository's commit_history, and update head_commit_hash .
2. checkout(repo, commit_hash): Implement 'Dirty State' prevention. Compare the current working_directory with the HEAD commit's snapshot. If they don't match, throw an error . If clean, replace working_directory with the target node's snapshot.
3. reset(repo): Forcefully clear the undo_stack and redo_stack, then overwrite working_directory with the HEAD snapshot.*/