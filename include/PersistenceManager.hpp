/*Implement the PersistenceManager class to handle serialization.  save_all_state(): Use atomic file writing. Write to a .tmp file first, flush, close, and then use std::filesystem::rename to overwrite data/users.txt, repos.txt, and commits.txt.  Implement strict escaping: Before writing, replace \ with \\, and replace | with \|. Multi-line strings in file snapshots must be written using literal \n.
3. load_all_state(): Reconstruct maps and chains on startup. It MUST be fault-tolerant: use a try-catch block inside the parsing loop to safely discard malformed lines without crashing the application .*/